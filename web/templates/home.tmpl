<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ultimate Jogo da Velha</title>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/home.css">
</head>

<body>

    <div class="bg-orb orb-1"></div>
    <div class="bg-orb orb-2"></div>

    <main class="login-card">
        <h1 class="game-logo"><b>Ultimate</b> Jogo da Velha</h1>
        <p class="subtitle">Desafie seus amigos em partidas épicas</p>

        <form action="/play" method="POST" id="gameForm">
            <div class="input-group nickname-section">
                <label for="nickname" class="input-label">Identificação</label>
                <input type="text" id="nickname" name="nickname" placeholder="Digite seu nickname..." required>
            </div>

            <div class="action-section">
                <button type="submit" class="btn-play btn-create" onclick="setCreateMode()">
                    CRIAR NOVA SALA
                </button>

                <div class="divider">
                    <span>OU</span>
                </div>

                <div class="input-group">
                    <input type="text" name="room_code" placeholder="Tem um código? Digite aqui" required>
                </div>
                <button type="submit" class="btn-play btn-join">
                    ENTRAR NA SALA
                </button>
            </div>
        </form>

        <script>
            function setCreateMode() {
                const codeInput = document.querySelector('input[name="room_code"]');
                // Temporarily disable so it's not sent, allowing Create logic (empty/missing code) to work
                // and bypassing 'required' validation for this field.
                codeInput.disabled = true;

                setTimeout(() => {
                    codeInput.disabled = false;
                }, 100);
            }
        </script>
    </main>

</body>

</html>